# EXTREMELY SIMPLE REDIRECTS - NO LOOPS

# Asset paths - direct pass through
/assets/* /assets/:splat 200
/functions/* /functions/:splat 200

# Direct HTML access - no redirects
/*.html /:splat 200

# Root path goes to index
/ /index.html 200

# Everything else is 404
/* /index.html 404
